# Runtime dependencies — everything needed to actually run NOVA-VAD
# (src/vad.py, src/classifier.py, src/denoiser.py, src/explainer.py,
#  src/pipeline.py, src/stream.py, record_dataset.py, retrain_streaming.py).
# Does NOT include the benchmark-only baselines (Silero/Pyannote/SpeechBrain)
# or research-only tooling (SHAP, PyTorch neural experiment) — see
# requirements-benchmark.txt and requirements-research.txt for those.

numpy==2.4.6
scipy==1.18.0
scikit-learn==1.9.0
joblib==1.5.3
librosa==0.11.0
audioread==3.1.0
numba==0.65.1
llvmlite==0.47.0
soxr==1.1.0
pooch==1.9.0
lazy-loader==0.5
decorator==5.3.1
msgpack==1.2.1
soundfile==0.14.0
webrtcvad==2.0.10
noisereduce==3.0.3
sounddevice==0.5.5
