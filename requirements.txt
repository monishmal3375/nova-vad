# Umbrella file kept for backward compatibility — installs everything
# (runtime + research + benchmark + dev), same as before this split.
#
# For a much smaller install, pick only what you need:
#   pip install -r requirements-runtime.txt     # just run NOVA-VAD
#   pip install -r requirements-research.txt     # + SHAP, neural_vad.py experiment
#   pip install -r requirements-benchmark.txt    # + Silero/Pyannote/SpeechBrain comparison
#   pip install -r requirements-dev.txt          # + pytest for the test suite

-r requirements-runtime.txt
-r requirements-research.txt
-r requirements-benchmark.txt
-r requirements-dev.txt
